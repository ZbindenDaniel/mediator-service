// TODO(agent): Remove frontend wrapper once all category lookup imports use models directly.
export type { ItemCategoryLookups, ItemSubcategoryWithParent } from '../../../models/item-category-lookups';
export { buildItemCategoryLookups } from '../../../models/item-category-lookups';
