# Project Overview

Runbooks: [docs/detailed/](docs/detailed/README.md) · Changelogs: [docs/changelogs/](docs/changelogs/README.md)

## Current focus
- Stabilize ERP sync by removing unproven continuation heuristics and preserving only behavior backed by known request evidence.
- Harden pricing-agent JSON reliability by repairing malformed model output before schema validation.

## System map

| Area | README | Primary changelog |
|---|---|---|
| Backend server | [backend/](backend/README.md) | — |
| Agentic pipeline | [backend/agentic/](backend/agentic/README.md) | [agentic](docs/changelogs/agentic.md) |
| API action handlers | [backend/actions/](backend/actions/README.md) | — |
| Frontend SPA | [frontend/](frontend/README.md) | [ui](docs/changelogs/ui.md) |
| Shared models | [models/](models/README.md) | — |
| Runtime contracts | [contracts/](contracts/README.md) | [item-lifecycle](docs/changelogs/item-lifecycle.md) |
| Print server | [cups/](cups/README.md) | [printing](docs/changelogs/printing.md) |
| Reference docs | [docs/detailed/](docs/detailed/README.md) | — |

## Topic changelogs

| Topic | File | Covers |
|---|---|---|
| Item lifecycle | [item-lifecycle.md](docs/changelogs/item-lifecycle.md) | Item CRUD, quality, specs, accessories, spare parts, CO₂ |
| Agentic pipeline | [agentic.md](docs/changelogs/agentic.md) | AI enrichment, extraction, review flow, dispatch queue |
| ERP sync | [erp-sync.md](docs/changelogs/erp-sync.md) | ERP import/export, CSV, Langtext, nightly sync, Shopware |
| Printing | [printing.md](docs/changelogs/printing.md) | Labels, CUPS, printer queues, drivers |
| Media & files | [media.md](docs/changelogs/media.md) | Photos, attachments, external docs, WebDAV |
| Storage & boxes | [storage.md](docs/changelogs/storage.md) | Boxes, locations, relocation, stubs, placement |
| Device intake | [intake.md](docs/changelogs/intake.md) | Intake station, netboot, cataloguing flow |
| Scanning & QR | [scanning.md](docs/changelogs/scanning.md) | QR generation, scanner workflows, audit |
| UI/UX | [ui.md](docs/changelogs/ui.md) | Frontend layout, navigation, help pages |
| Testing | [testing.md](docs/changelogs/testing.md) | Test coverage, test rewrites, test infrastructure |
| Docs & infra | [docs-infra.md](docs/changelogs/docs-infra.md) | Documentation, config, Docker, DB migrations |

## Recent changes (last 10)

946. ✅ Fix Shopware **configurator 1062 recurrence** — reuse existing configurator-setting ids. #944's deterministic id stopped *new* duplicates but couldn't heal rows created before ids were set: a PATCH entry with a fresh id for an `(product, option)` that already has a row makes Shopware **INSERT** → `1062 Duplicate entry` on `uniq.product_configurator_setting` again (hit once the itemUUID axis was added, since the PATCH now carries options that already had rows). `syncVariants` now first searches `product-configurator-setting` for the parent, **reuses the existing row's id** per option (so the PATCH updates in place), gives only genuinely-new options a deterministic id, and deletes stale axes (404-tolerant). Verified 12/12 (added: existing option reuses its stored id, new option gets a deterministic id, stale axis deleted). → [erp-sync]
945. ✅ Shopware sync: **image previews render** + **itemUUID as the variant axis**. (1) **Blank previews:** uploaded product images showed no preview because `ensureMedia` created the `media` entity with **no folder** — unassigned media gets no thumbnail config, so Shopware never generates thumbnails. It now resolves the **Product Media** default folder once (`/api/search/media-folder` on `defaultFolder.entity=product`, cached) and sets `mediaFolderId` on create, so thumbnails/previews generate (falls back to unassigned if unresolved). (2) **itemUUID axis:** `buildVariantGroups` now adds each instance's `ItemUUID` as a variant option, so the signature is unique per instance → **one variant = one physical item** (instances no longer merge), letting the operator switch variants on `itemUUID` in Shopware and group the spec axes as descriptive. Consequence: any ref with **≥2 instances** is now a variant parent (spec-less instances included); a single-instance ref stays a plain product. Verified: grouping 6/6 (per-instance, no merge, itemUUID present) + images 8/8 (folder resolved+set, cached) + variants 9/9 regression. → [erp-sync]
944. ✅ Fix Shopware **variant re-sync 500** (duplicate configurator entry): `adminClient.syncVariants` PATCHed the parent's `configuratorSettings` with entries that had **no `id`**, so every re-sync re-inserted the same option rows and hit `uniq.product_configurator_setting` (product+version+option) → `1062 Duplicate entry` (500 = retryable ⇒ the manual Shop-Sync of an existing variant parent wedged in a retry loop). Each entry now carries a **deterministic id** (`sha1(parentId:optionId)`, same idempotency trick as product_media) so a re-sync upserts the same row by primary key instead of duplicating. Verified 9/9 (incl. new deterministic-id + identical-ids-across-re-sync assertions). The image `mkdir Permission denied` the operator also saw is a dockware container fs-permission issue (code already skips a failed image), and the variant-storefront grouping is a Shopware-side presentation concern — both out of scope here. → [erp-sync]
943. ✅ Shopware **image sync** (P3): product images uploaded as **binary** (mediator→Shopware only). `lib/shopware-media.resolveShopwareImageInputs` maps a ref's `Grafikname` (cover) + `ImageNames` (`|`-list) to on-disk files (folder = 6-digit Artikel_Nummer) with lazy byte loaders + deterministic media filenames. `adminClient.syncProductImages` (in `upsertProduct`, on the parent): `ensureMedia` searches `media` by fileName → reuse, else create empty media + `POST /api/_action/media/{id}/upload` (raw bytes); then PATCH `product.media` (deterministic product_media ids) with `coverId`=first. Variant children inherit parent media; a failed image is skipped. Verified 5/5 (create+upload / reuse / cover / link); regression intact. Validate the media m2n + coverId write on a live shop; stale-image removal + documents deferred (doc options in the changelog). → [erp-sync]
942. ✅ Shopware: **quality as a variant axis** + **manual Shop-Sync button** (admin). (1) `buildVariantGroups` injects each instance's quality label (`describeQuality(items.Quality)` → Ersatzteil/…/Neuwertig) as a `Zustand` variant option (unknown skipped), so instances differing only in condition become separate variants (and quality-only variants work without InstanceSpecs); no storage change. (2) `POST /api/admin/shopware/sync` (admin-gated): body `{itemIds}` → sync selected items' refs (list bulk-action `enqueueShopwareSyncForItems`), no body → all `Shopartikel=1` refs (backfill). Frontend: a **"Shop Sync 🛒"** bulk action in `BulkItemActionBar` (next to Kivi Sync) shown **only when the admin token is present** (logged into admin page), sent as Bearer; plus "Alle synchronisieren" on the admin card. Dispatch gate syncs approved / deactivates ineligible, so it also backfills old skeletons. Verified quality grouping 4/4. Contracts/CO₂ intentionally skipped. Stock-back (orders→book-out) is the open design item. → [erp-sync]
941. ✅ Shopware **variants** (P2c): ref → **parent product**, distinct **InstanceSpecs** combos → **variant children**. Maps the mediator's ref/instance relation onto Shopware's parent/variant model (schema already had `ShopwareProductId`/per-instance `ShopwareVariantId`). Because Shopware requires unique option combos, a variant = one distinct InstanceSpecs combination with stock = summed matching instances (identical units merge → stock N; unique specs ⇒ one variant per instance). `db.buildVariantGroups` groups instances by canonical spec signature (used only when ≥2 combos AND every group has options; else single-product). `adminClient.syncVariants`: resolve combos → property options (reusing P2a), PATCH parent `configuratorSettings`, upsert one child per combo (`parentId`+options+summed stock, inheriting price/tax/visibility), retire vanished combos (`active:false,stock:0`); each instance's `ShopwareVariantId` persisted via `syncClient`. Verified: grouping 4/4 + variant flow 7/7 + gate/prop regression. Shopware `configuratorSettings`/child-`options` write semantics to validate on a live shop (as with P2a m2n). → [erp-sync]
940. ✅ Shopware sync: **shop-eligibility gate + full product data + visibility** (live-test fixes). (1) **Gate:** unreviewed/non-shop items were landing in Shopware — the snapshot now computes `shopEligible = Shopartikel=1 AND agentically approved` (same approval as the ERP gate; scalar subselects so multi-run refs don't corrupt the stock SUM). Ineligible → **deactivated** if it exists (`active:false`), else **skipped**; never published. (2) **published→active:** `Veröffentlicht_Status='yes'` maps to product `active`. (3) **Full data:** the update path PATCHed stock only — now a shared `buildProductFields` writes name/description/price(net from tax, sales-channel currency)/active/stock on create AND update (specs/properties already worked). (4) **Visibility:** create now sets `visibilities:[{salesChannelId,visibility:30}]` (channel resolved once via `resolveSalesChannel` by accessKey → id+currencyId) so a published+active product actually appears in the storefront. `ShopwareUpsertResult.action` gained `deactivated`/`skipped`. Verified 10/10 gate + 3/3 property. Deferred: items without agentic approval never sync (matches ERP gate); pre-existing skeletons self-correct only on next enqueue; visibility on create only; images P3. → [erp-sync]
939. ✅ Fix Shopware create-product 500: `resolveCurrencyId` filtered `/api/search/currency` on `isSystemDefault`, a **runtime field** Shopware rejects in a criteria (`FRAMEWORK__RUNTIME_FIELD_IN_CRITERIA`). Only bit the **create** path (updates just PATCH stock), so ~half the items synced before the first new product wedged all further new-product syncs (500=retryable). Now resolves currency from the **sales channel** (`/api/search/sales-channel` filtered on `accessKey`, a real field) → its `currencyId` (correct per-channel, e.g. CHF for a Swiss shop); falls back to the seeded system-default currency id **uncached** so a transient failure can't pin the wrong currency. Failed jobs retry to success after redeploy. Verified 17/17. → [erp-sync]
938. ✅ Shopware **property sync** (P2a): Langtext/Spezifikationen → **filterable properties**. Operator insight: use Shopware property groups+options (native storefront filters), not custom fields. Each structured Langtext entry (`Record<string,string|string[]>`, keys `Prozessor`/`Display`/`Anschlüsse`/…) maps to a property group → option(s). `ShopwareAdminClient.upsertProductStock`→`upsertProduct` now also `syncProductProperties`: `ensurePropertyGroup` (create `filterable:true` if missing) + `ensurePropertyOption` (both cached across jobs), then reconciles the product's associations to **exactly** the desired options — robust to either Shopware write semantic (PATCH full desired list, then 404-tolerant DELETE of stale links); array values → multiple options. The DB snapshot now parses `item_refs.Langtext` and passes the object form as `properties` (freeform prose → none). Property errors share the retry classification; stock is set first so a property hiccup never loses stock, and retries are idempotent. Verified: P1 suite 15/15 post-rename + new property cases (create/link/remove-stale, array→2 options under reused group). Deferred: contract-driven curation of which keys are filterable; translations/units/position. → [erp-sync]
934. ✅ Creating another instance of an existing reference no longer restarts an `approved`/`review` agentic run. `import-item` already declines to re-seed when a run exists, but the frontend then fired its own `POST /api/agentic/run`, and `forwardAgenticTrigger` restarted every non-active run (incl. approved/review), clobbering the result. It now restarts only a `notStarted` run from that automatic path; active/settled runs are returned untouched. Explicit restarts (dedicated `…/agentic/restart`) are unaffected. → [agentic]
