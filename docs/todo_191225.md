


## directly export one item with new sync feature
 
Based on the provided shell script implement the needed changes to allow to import files to the ERP via the CLI. 
Add a new action or refactor 'export-items'. the goal is to be able to sync items with the ERP application. This is possible via a curl command through the CLI. I would like this to ba an action in the item List where we can sync one ore multilple item with the ERP.

## localStorage filter settings, display in header

Adding localStorage might improve the user experience. I would like to save the filters from the itemList in the localstorage. When a filter is in the localstorage we display a filter Icon in the header bar. onHover on that icon the filters are displayed, onClick the filters are cleared.


## enum names in export

In the curent export we export the numeric values of the category enums. The erp expects the string names present in the format present in data_struct.md (display values with underscores replacing spaces)


## export headers

In the CSV export we curently export the headers in the intenral format instead of the expected format of the ERP. this is the target header format:

```
Artikel-Nummer,	Artikeltyp,	CreatedAt,	Grafikname(n),	Artikelbeschreibung	Kurzbeschreibung,	Langtext,	Hersteller,	Länge(mm),	Breite(mm),	Höhe(mm),	Gewicht(kg),	Verkaufspreis,	Auf_Lager,	Veröffentlicht_Status,	Shopartikel	Einheit	EAN	Hauptkategorien_A_(entsprechen_den_Kategorien_im_Shop),	Unterkategorien_A_(entsprechen_den_Kategorien_im_Shop),	Hauptkategorien_B_(entsprechen_den_Kategorien_im_Shop),	Unterkategorien_B_(entsprechen_den_Kategorien_im_Shop),	itemUUID,	BoxID,	Location,	UpdatedAt
```


## quality field 1-5, > 3 = shop

We need to add a new field to 'itemInstance'. This field is 'Quality' its a number range from 1 to 5. Default value 3. 

For this we need to add the following UI additons:

- in the basic form: a slider from 1-5 which sets this value
- item-Detail: a badge with the value next to the first row with the itemUUID. the badge is colored purple, red, yellow, orange, green
- item-edit: a slider to modify the quality
- itemList: a new filter which filter quality >= the value, and also sort by quality
- itemList: a column with the quality

We need this logic aswell:

- Update logic: if quality < 3 then Shopartikel = 0 else Shopartikel = 1


## keine html paths als media exportieren

at the moment the transcript.htmlfiles are part of the media string in the export. this is not okay. 


## easy price update

When an item is reviewd we set the price if it is not set already based on a lookup table.

## set fields on trigger

if agentic review is not in state reviewed then we export Veröffentlicht_Status = false. this means the exported value for Veröffentlicht_Status is: Veröffentlicht_Status && agenticStatus == 'reviewed'

## Event Log levels

We introduces event levels and topics to filter out based ont environmental seetings but I do not see the possibility to aply those setings. There should be two environmental settings. One for which define the topics to display and one to set the log level. check if this is done already, if yes leave it as is and explain to me how to set the variables. if not implement it like this: Use localStorageto keep track of the settings 

