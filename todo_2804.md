

- we should exclude the current instance from the Vorrat list.so vorrat becomes 'other items' to prevent confusion
- remove detail-liste link in box-itemList. . not needed in new paradigm
- erfasst von? we should display this
- improving event logs
- general restructuring of events (relocated from to by, angepasst: Preis, inventory, )



- print modal is still transparent. It's overwritten by '.app-shell__right .card'
- print outputs (pdf) are not displayed. shoud be a link in the tab after the print job. maybe we call no the same print modal? when pcreating an item the modal shows up fine and displays the created pdf inside the modal. when going through 'label drucken' the modal is different and closes directly 
- in AI tab some buttons are still at the bottom. Ki status should be even above the button group
- box item list still has buttons not in the button group.
- box info tab with better summary (number and weight of items)
- box item list does not need Behälter column
- we need a way to go back. the browser hstroy is keeping track of the detail tap state but it is not rendered when going through it.
- make first and last button in group with border-radius
- the search in the nav does not use the search functionallity we implemented? Is it just a link to the item List? this should basically be the same as the search card before
- after item edit we still land in the behaviour where things get rendered to the left
- the 'landing page' should still be the the item list with the new item form to the right. meaning redirect to 'http://localhost:8080/items?entity=item&tab=create'




