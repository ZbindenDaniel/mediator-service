{
  "version": 1,
  "subCategory": 109,
  "fields": [
    { "key": "Prozessor",       "required": true,  "description": "CPU model and speed" },
    { "key": "Bildschirmgröße", "required": true,  "description": "Integrated display size in inches (e.g. 21.5, 23.8, 27)" },
    { "key": "Auflösung",       "required": false, "description": "Display resolution (e.g. 1920×1080, 2560×1440, 4K)" },
    { "key": "Touchscreen",     "required": false, "description": "Touchscreen support (Ja/Nein)" },
    { "key": "Speichertyp",     "required": false, "description": "Drive type (SSD, NVMe SSD, HDD)" },
    { "key": "Speicher",        "required": false, "description": "Storage capacity (e.g. 512GB, 1TB)" },
    { "key": "RAM-Kapazität",   "required": false, "description": "Installed / max RAM (e.g. 16GB)" },
    { "key": "RAM-Typ",         "required": false, "description": "Type of RAM (DDR4, DDR5)" },
    { "key": "Grafikkarte",     "required": false, "description": "Discrete GPU model, if any (otherwise integrated)" },
    { "key": "Anschlüsse",      "required": false, "description": "Interfaces like USB, HDMI, Ethernet, etc." }
  ],
  "guidance": [
    "All-in-One PCs integrate the display into the chassis — always capture the screen size (and resolution when visible).",
    "Do not mention the operating system in the prose."
  ]
}
