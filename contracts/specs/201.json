{
  "version": 1,
  "subCategory": 201,
  "fields": [
    { "key": "Prozessor",       "required": true,  "description": "CPU model and speed" },
    { "key": "Display",         "required": false, "description": "Screen size and resolution" },
    { "key": "Anschlüsse",     "required": false, "description": "Different interfaces like USB, HDMI, Ethernet, etc." }
  ],
  "guidance": [
    "Do not mention the operating system or the specific drive/disk model in the prose description (Kurzbeschreibung) or specs; those may vary inbetween the same model and can not be guaranteed"
  ]
}
