{
  "version": 1,
  "subCategory": 105,
  "fields": [
    { "key": "Prozessor",       "required": true,  "description": "CPU (e.g. Apple M2, Intel Core i7)" },
    { "key": "Display",         "required": false, "description": "Screen size and resolution (MacBook/iMac only)" },
    { "key": "RAM-Slots",       "required": false, "description": "number of Ram Slots (e.g. 1, 2, 4)" },
    { "key": "RAM-Kapazität",       "required": false, "description": "max. RAM (e.g. 16GB)" },
    { "key": "RAM-Typ",            "required": false, "description": "Type of RAM (DDR3, DDR4, DDR5)" },
    { "key": "Anschlüsse",     "required": false, "description": "Different interfaces like USB, HDMI, Ethernet, etc." }

  ]
}
