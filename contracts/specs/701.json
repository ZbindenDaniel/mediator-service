{
  "version": 1,
  "subCategory": 701,
  "fields": [
    { "key": "Grafikspeicher", "required": true,  "description": "VRAM amount (e.g. 8 GB GDDR6)" },
    { "key": "Schnittstelle",  "required": true,  "description": "Bus interface (e.g. PCIe 4.0 x16)" },
    { "key": "Chipsatz",       "required": false, "description": "GPU chip model (e.g. GA106)" },
    { "key": "TDP",            "required": false, "description": "Power draw in watts" },
    { "key": "Anschlüsse",     "required": false, "description": "Display outputs (e.g. 3x DP 1.4, 1x HDMI 2.1)" }
  ]
}
